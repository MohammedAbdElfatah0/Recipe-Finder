import 'package:flutter/material.dart';

class FavortireScreen extends StatelessWidget {
  const FavortireScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Favortire Screen'),
      ),
    );
  }
}